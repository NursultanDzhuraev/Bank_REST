spring:
  application:
    name: KuimaKulak
  datasource:
    url: jdbc:postgresql://localhost:5432/bank_rest
    username: postgres
    password: nurs123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
  security:
    jwt:
      secret: bank_rest001100110011$$
  liquibase:
    change-log: db/changelog/main-changelog.yaml
    enabled: true


server:
  port: 8282

